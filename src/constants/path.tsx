const path = '/react-threejs-examples/pisa/';
const format = '.png';

export const Urls = [
    path + 'px' + format, path + 'nx' + format,
    path + 'py' + format, path + 'ny' + format,
    path + 'pz' + format, path + 'nz' + format
];

export const ModelPath = '/react-threejs-examples/burger.glb';